<h1>projects</h1>

{{#each updatedUserData as |user| }}
<div class= "row">
<div class="col-6">
  <h1>{{user.name}}</h1>
</div>
<div class="col-6">
<h2>{{user.email}}</h2>
</div>
<div class="col-6">
  {{user.github}}
</div>
</div>
{{/each}}